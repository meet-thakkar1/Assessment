Started With Health probe and other probes probes without authentication

It crealy return 404 and 401 for different endpoint.

So Without Authentication I discovered Endpoints which returned 401.

Figuredout below Endpoints

/api/v1/dataset
/api/v1/submit